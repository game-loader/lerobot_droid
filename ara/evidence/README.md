# Migration Evidence

- `migration-pytest.xml`: unmodified final JUnit result generated by
  `bash scripts/test_custom_version.sh --junitxml=/tmp/lerobot-droid-migration-final.xml`.
  Includes subtests in the XML count; terminal summary was 1,246 passed,
  21 skipped, 2 deselected, 20 subtests passed.
- `../../docs/CUSTOM_VERSION_VALIDATION.md`: concrete test scope, initial gated
  failures, isolated-base validation, wheel checks and checkpoint smoke observations.
- `../src/execution/checkpoint_smoke.py`: exact bounded harness used for
  read-only production checkpoint loading and one-step predictions; source artifacts
  remain external and are not embedded in the research record.

The final test result is a software compatibility measurement, not simulator or
real-robot success-rate evidence. No model weights or training logs are included.
